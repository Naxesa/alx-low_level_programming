#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0(Success)
 * Printf: Prints the string
 */
int main(void)
{
	printfprintf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
